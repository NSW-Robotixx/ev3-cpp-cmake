#include <gtest/gtest.h>

#include <server/webServer/webComponents/WebComponentBase.hpp>

// finder::server::webComponents::WebComponentBase WC = finder::server::webComponents::WebComponentBase{};


TEST(WebComponentBase, Constructor)
{
}